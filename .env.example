# Database
# SQLite (development)
DATABASE_URL="file:./dev.db"

# PostgreSQL (production) - use schema.postgresql.prisma
# DATABASE_URL="postgresql://user:password@localhost:5432/taskflow?schema=public"

# Auth (required - server won't start without it)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Optional: stable hash for log correlation (e.g. invalid_role_rejected) without PII. Independent of JWT rotation.
# If unset, emailHash is omitted from structured logs in production.
# LOG_HASH_SECRET="your-log-hash-secret"

# Cron: required to enable the /api/cron/daily-tasks endpoint
# If not set, the endpoint returns 503 (disabled)
CRON_SECRET="your-cron-secret-for-daily-task-assignment"

# Environment
NODE_ENV="development"

# Server port (default 3000 in production)
# PORT=8080

# When app is served over HTTP only (no HTTPS), set to "false" so cookies (auth, CSRF) are sent
# Required for login/CSRF to work on http://IP:3000. Set "true" or omit when using HTTPS (e.g. nginx).
# COOKIE_SECURE=false

# Reverse proxy: set to "true" when behind nginx/load balancer (required for correct IP in rate-limit)
# TRUST_PROXY=true

# CORS (production only) - comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS="https://app.example.com,https://admin.example.com"
# If unset in production, no origins are allowed. In development, localhost is permitted.
# ALLOWED_ORIGINS=
